# 🎨 Design-Agent 设计需求管理系统

一个基于 **AI 对话式交互** 的设计需求管理平台，通过智能 Agent 自动采集需求、推荐设计规范、管理任务流程。

![Python](https://img.shields.io/badge/Python-3.11+-blue?logo=python)
![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-green?logo=fastapi)
![React](https://img.shields.io/badge/React-18+-61DAFB?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5+-3178C6?logo=typescript)

---

## ✨ 核心功能

### 🤖 Multi-Agent 智能助手
- **意图识别路由**：Supervisor Agent 自动识别用户意图（创建/查询/管理），路由到专职 Agent
- **对话式需求采集**：Creator Agent 用自然语言采集需求，自动提取关键信息
- **RAG 知识检索**：Query Agent 基于向量数据库检索设计规范（5类规范文档）
- **智能需求管理**：Manager Agent 支持更新/取消需求，Tool Calling 自动调用工具
- **实时表单同步**：AI 提取的信息实时同步到右侧需求单表单

### 📋 任务看板（五状态流转）
```
待接单 → 进行中 → 待验收 → 修改中 → 已完成
```
- **拖拽式管理**：清晰的看板视图，一目了然
- **角色操作分权**：设计师可接单/提交，运营可验收/退回
- **状态自动流转**：操作后自动更新任务状态

### 👥 多角色权限系统
| 角色 | 权限 |
|------|------|
| **管理员** | 全局查看、用户管理、数据统计 |
| **运营** | 发起需求、指派设计师、验收成果 |
| **设计师** | 接收任务、提交作品、查看自己的任务 |

### 📊 效能统计报表
- 设计师工作量统计
- 任务完成率分析
- 按时间段筛选数据

### 💾 数据持久化
- 对话记录自动保存到 localStorage
- 需求单表单自动保存
- 切换页面后数据不丢失

---

## 🛠️ 技术架构

### 后端技术栈
| 技术 | 用途 |
|------|------|
| **FastAPI** | 高性能异步 Web 框架 |
| **SQLAlchemy** | ORM 数据库操作 |
| **SQLite + aiosqlite** | 异步轻量级数据库 |
| **PostgreSQL + pgvector** | 向量数据库（RAG 检索） |
| **LangChain** | LLM 应用开发框架 |
| **LangGraph** | Multi-Agent 工作流编排 |
| **通义千问 (Qwen)** | AI 大语言模型 |
| **JWT** | 用户认证 |
| **WebSocket** | 实时双向通信 |

### 前端技术栈
| 技术 | 用途 |
|------|------|
| **React 18** | UI 框架 |
| **TypeScript** | 类型安全 |
| **Vite** | 构建工具 |
| **Ant Design** | UI 组件库 |
| **Zustand** | 状态管理 |
| **Axios** | HTTP 请求 |

### Multi-Agent 系统架构
```
┌─────────────────────────────────────────────────────────┐
│                   Frontend (React)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │ 需求采集页  │  │  任务看板   │  │   效能统计     │  │
│  └──────┬──────┘  └──────┬──────┘  └────────┬────────┘  │
└─────────┼────────────────┼──────────────────┼───────────┘
          │ WebSocket      │ REST API         │
          ▼                ▼                  ▼
┌─────────────────────────────────────────────────────────┐
│                   Backend (FastAPI)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │  Chat API   │  │ Tasks API   │  │  Reports API    │  │
│  └──────┬──────┘  └─────────────┘  └─────────────────┘  │
│         │                                                 │
│         ▼                                                 │
│  ┌────────────────────────────────────────────────────┐ │
│  │           Multi-Agent Orchestrator                 │ │
│  └────────────────────────────────────────────────────┘ │
│         │                                                 │
│         ├──────────┬──────────┬──────────┐              │
│         ▼          ▼          ▼          ▼              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │Supervisor│ │ Creator  │ │  Query   │ │ Manager  │  │
│  │  Agent   │ │  Agent   │ │  Agent   │ │  Agent   │  │
│  │(意图路由)│ │(需求创建)│ │(RAG检索) │ │(状态管理)│  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
└─────────────────────────────────────────────────────────┘
          │                │              │
          ▼                ▼              ▼
┌─────────────────┐  ┌─────────────┐  ┌─────────────────┐
│    SQLite DB    │  │  Qwen API   │  │  PostgreSQL     │
│  (需求/用户)    │  │    (LLM)    │  │  + pgvector     │
└─────────────────┘  └─────────────┘  └─────────────────┘
```

---

## 🚀 快速开始

### 环境要求
- Python 3.11+
- Node.js 18+

### 1. 克隆项目
```bash
git clone https://github.com/huahuahua0126/design-agent.git
cd design-agent
```

### 2. 配置 API Key
```bash
echo "QWEN_API_KEY=your_api_key_here" > backend/.env
```

### 3. 启动后端
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8080
```

### 4. 启动前端
```bash
cd frontend
npm install
npm run dev
```

### 5. 访问系统
- **前端页面**: http://localhost:5173
- **API 文档**: http://localhost:8080/docs

---

## 👥 测试账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | 123456 | 管理员 |
| operator | 123456 | 运营 |
| designer | 123456 | 设计师 |

---

## 📁 项目结构

```
design-agent/
├── backend/
│   ├── app/
│   │   ├── api/              # API 路由
│   │   │   ├── auth.py       # 认证接口
│   │   │   ├── chat.py       # Agent 对话
│   │   │   ├── requirements.py # 需求管理
│   │   │   ├── tasks.py      # 任务操作
│   │   │   └── reports.py    # 统计报表
│   │   ├── agent/            # Multi-Agent 系统
│   │   │   ├── supervisor_agent.py   # Supervisor（意图路由）
│   │   │   ├── creator_agent.py      # Creator（需求创建）
│   │   │   ├── query_agent.py        # Query（查询检索）
│   │   │   ├── manager_agent.py      # Manager（状态管理）
│   │   │   ├── multi_agent_workflow.py # 编排器
│   │   │   ├── tools.py              # Tool 定义
│   │   │   └── rag_store.py          # RAG 向量存储
│   │   ├── core/             # 核心配置
│   │   │   ├── config.py     # 配置管理
│   │   │   ├── database.py   # 数据库连接
│   │   │   └── security.py   # 安全认证
│   │   └── models/           # 数据模型
│   │       ├── requirement.py
│   │       └── user.py
│   ├── data/                 # 数据目录
│   │   └── design_specs/     # 设计规范知识库
│   │       ├── banner_specs.md
│   │       ├── poster_specs.md
│   │       ├── detail_page_specs.md
│   │       ├── icon_specs.md
│   │       └── general_specs.md
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── RequirementChat/  # 需求采集页
│   │   │   ├── TaskBoard/        # 任务看板
│   │   │   ├── Reports/          # 效能统计
│   │   │   └── Login/            # 登录页
│   │   ├── services/api.ts   # API 服务
│   │   └── stores/           # 状态管理
│   └── package.json
├── start.sh                  # 一键启动脚本
└── README.md
```

```

---

## 🌟 Multi-Agent 功能演示

### 场景 1：创建需求
```
用户：我需要一个双11促销Banner
↓ Supervisor 路由到 Creator Agent
Creator：好的！我来帮您创建这个需求。

根据Banner设计规范，建议：
- 尺寸：1080x640 或 750x400
- 文字不超过20字
- 使用品牌主色调

请问Banner的具体尺寸是？
```

### 场景 2：查询规范（RAG 检索）
```
用户：Banner设计有什么规范
↓ Supervisor 路由到 Query Agent
↓ Query Agent 调用 search_design_specs Tool
↓ RAG 语义检索设计规范知识库
Query：查询到以下设计规范：

【规范 1】
首页 Banner 建议尺寸：1080x640 或 750x400
文字总量不超过 20 字...
```

### 场景 3：查询需求列表
```
用户：我有哪些需求
↓ Supervisor 路由到 Query Agent
↓ Query Agent 调用 query_requirements Tool
Query：找到 3 条需求：

- [1] 双11促销Banner
  类型：banner，状态：待接单
  创建时间：2024-12-17 10:00
```

### 场景 4：更新需求
```
用户：把需求1的标题改成"双12促销Banner"
↓ Supervisor 路由到 Manager Agent
↓ Manager Agent 调用 update_requirement Tool
Manager：已成功将需求 [1] 双11促销Banner 的标题更新为：双12促销Banner
```

### 场景 5：取消需求
```
用户：取消需求1
↓ Supervisor 路由到 Manager Agent
↓ Manager Agent 调用 cancel_requirement Tool
Manager：已成功取消需求 [1] 双12促销Banner（原状态：待接单）
```

---

## 📝 技术说明

### Multi-Agent 实现方式

本项目的 Multi-Agent 系统包含以下组件：

1. **SupervisorAgent**：使用 LLM 的 structured output 功能进行意图分类，支持 4 种意图（create/query/manage/chat）
2. **CreatorAgent**：基于 LangChain 的对话式流程，包含需求提取、RAG 检索、完整性检查等节点
3. **QueryAgent**：基于关键词匹配触发不同工具调用（query_requirements/search_design_specs/get_task_status）
4. **ManagerAgent**：基于正则表达式解析用户指令，调用对应工具（update_requirement/cancel_requirement）

### 为何使用 pgvector

- **兼容性**：PostgreSQL 生态成熟，pgvector 扩展易于安装和使用
- **数据集成**：业务数据和向量数据可以在同一个数据库，降低运维复杂度
- **功能完整**：支持向量相似度检索、元数据过滤、ACID 事务等企业级特性
- **轻量部署**：不需要额外的向量数据库服务，一个 PostgreSQL 即可满足需求

### 简历建议

本项目可作为 **Agent Development Engineer** 简历项目，突出以下亮点：

- ✅ Multi-Agent 协作架构（Supervisor + 专职 Agent）
- ✅ RAG 知识检索系统（PostgreSQL + pgvector 向量数据库）
- ✅ LLM 驱动的意图识别与路由
- ✅ Tool Calling 集成（5 个可调用工具）
- ✅ 实时 WebSocket 通信
- ✅ 状态管理与对话历史上下文

---

## 🔮 后续计划

- [x] Multi-Agent 协作架构
- [x] RAG 知识库检索
- [x] Tool Calling 集成
- [ ] 图片上传到云存储（MinIO/OSS）
- [ ] 消息通知（企业微信/钉钉）
- [ ] 工时自动统计优化
- [ ] 移动端适配
- [ ] Docker 生产部署方案

---

## 📜 License

MIT License

---

## 🙏 致谢

- [FastAPI](https://fastapi.tiangolo.com/) - 高性能 Python Web 框架
- [LangChain](https://langchain.com/) - LLM 应用开发框架
- [Ant Design](https://ant.design/) - 企业级 UI 组件库
- [通义千问](https://dashscope.aliyun.com/) - 阿里云大语言模型
